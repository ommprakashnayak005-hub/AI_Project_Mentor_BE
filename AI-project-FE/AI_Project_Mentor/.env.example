# Default backend URL for the future FastAPI server.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Set to "true" to use built-in mock data (default).
# Set to "false" to call the real FastAPI backend instead.
VITE_USE_MOCK_DATA=true
